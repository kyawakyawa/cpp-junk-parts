# CPP JUNK PARTS

## Build
```bash
$ git submodule update --init --recursive --depth 1
$ bootstrap-ninja.sh
$ cd cmake-build-debug
$ ninja
```

## Update Submodule
```bash
$ git submodule update --remote
```