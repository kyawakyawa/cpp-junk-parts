# CPP JUNK PARTS

## Build
```bash
$ git submodule update --init --recursive --depth 1
$ bootstrap-make.sh
$ cd cmake-build-release
$ make -j
```

## Update Submodule
```bash
$ git submodule update --remote
```